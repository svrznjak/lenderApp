import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.svrznjak.lenderapp",
  appName: "Lender App",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
